document.addEventListener('DOMContentLoaded', function () {
	// Legacy quotes-custom functionality is disabled to avoid conflicts with the new Telegram-based flow
	// and to prevent requests to deprecated PHP endpoints. This file now acts as a no-op placeholder.
	console.log('quotes-custom.js: legacy features disabled (using Telegram login + new quotes flow).');
});
